--- 
title: "Ruby Array#each method secret"
layout: post
categories: ['en']
---
Array#each is one of the most popular method. Recently, I noticed that we can pass more then one parameters to block. It become very usefull, when element of array is also array. Then first block parameter become

